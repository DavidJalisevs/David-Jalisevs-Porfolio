#include "Timer.h"
#include <ctime>
#include <iostream>
#include <Windows.h>

void Timer::runTime(float timeByDifficulty)
{
    //system("cls");
    Sleep(1000);
   // timeTest++;

    timeLimit -= 1;
  
}

